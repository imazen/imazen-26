# cleanpicker-ladder11@2026-08-23 — byte-level successor of cleanpicker-ladder11@2026-06-26 (oxipng -o 4 --nx)

- **Files:** 4497 renditions — same names, same pixels, same containers as the
  predecessor; only the PNG filter/DEFLATE encoding changed.
- **Provenance:** every file is `oxipng 10.2.0 -o 4 --nx` (reductions disabled, no
  stripping) applied to the predecessor's bytes. Selection, split composition, and
  render lineage are the predecessor's — `selection.tsv` carried over verbatim;
  see `../cleanpicker-ladder11@2026-06-26/SET.md` for the full selection/render record.
- **Verified (2026-08-23, every file):** decoded pixel streams identical 4497/4497
  (`pngtopam | sha256` pairwise), container census identical (depth / colortype /
  interlace / full chunk sequence, incl. cICP/iCCP where present) — 0 deviations.
- **Bytes:** 1.02 GiB → **0.92 GiB** (ratio 0.897).
- **Storage:** `/mnt/v/output/imazen-26-variants/cleanpicker-ladder11@2026-08-23/` · LAN-store mirror
  `s3://codec-corpus/imazen-26-variants/cleanpicker-ladder11@2026-08-23/` (synced at registration) · NAS-array
  mirror PENDING (the operator box's NFS mount was stale at registration time) ·
  R2 sync at first cloud-consumer adoption.
- **Consumers:** none yet — adopt on each program's next wave. Existing datasets keep
  citing the predecessor (their sha256 references are to its bytes).
- **Status:** active.
